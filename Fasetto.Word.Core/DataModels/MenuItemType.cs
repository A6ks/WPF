namespace Fasetto.Word.Core
{
    /// <summary>
    /// The types of
    /// </summary>
    public enum MenuItemType
    {
        /// <summary>
        /// Shows the meny text and an icon
        /// </summary>
        TextAndIcon = 0,
        /// <summary>
        /// Shows a simple divider between the menu items
        /// </summary>
        Divider = 1,
        /// <summary>
        /// Shows the meny text as a header
        /// </summary>
        Header = 2,
    }
}
